% The following term definitions can be disabled by commenting them out,
% in which case the system defaults (English) will be used instead.
% Default for \prechaptername is "Chapter"
% Default for \postchaptername is an empty string
% Default for \tablename is "Table"
% Default for \figurename is "Figure"


%\renewcommand\prechaptername{Chapter } % appears at the start of each chapter as "Chapter x"
%\renewcommand\postchaptername{}

\ctexset{
    chapter = {
        name = {CHAPTER~},
        number = \arabic{chapter},
        aftername = {\par},
        format = \centering\Large\bfseries,
        titleformat = \MakeUppercase,
    },
    section = {
        format = \large\bfseries\raggedright,
        beforeskip = {1.5ex plus 0.5ex minus 0.2ex},
        afterskip = {1ex plus 0.2ex},
    },
    subsection = {
        beforeskip = {1.3ex plus 0.4ex minus 0.2ex},
        afterskip = {0.9ex plus 0.2ex},
    },
    subsubsection = {
        beforeskip = {1.1ex plus 0.3ex minus 0.2ex},
        afterskip = {0.7ex plus 0.2ex},
    },
}
\renewcommand{\tablename}{Table} % table captions in the document will appear as "Table x"
\renewcommand{\figurename}{Figure} % figure captions in the document will appear as "Figure x"

% The following term definitions are used for the fixed section names of the thesis
% (appearing in the table of contents and as page headings)
\newcommand{\nameInnerCover}{Advisor Recommendation}
\newcommand{\nameCommitteeForm}{Thesis Committee Approval Form}
\newcommand{\nameCopyrightForm}{Copyright Authorization}
\newcommand{\nameCabstract}{ABSTRACT (CHINESE)}
\newcommand{\nameEabstract}{ABSTRACT}
\newcommand{\nameAckn}{ACKNOWLEDGMENTS}
\newcommand{\nameToc}{TABLE OF CONTENTS}
\newcommand{\nameLot}{LIST OF TABLES}
\newcommand{\nameTof}{LIST OF FIGURES}
\newcommand{\nameToa}{LIST OF ALGORITHMS}
\newcommand{\nameSlist}{NOMENCLATURE}
\newcommand{\nameRef}{REFERENCES}
\newcommand{\nameVita}{CURRICULUM VITAE}
